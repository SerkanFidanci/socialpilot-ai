# Common Worker Area

Slice 0A provides the Celery application in `services/api/app/infrastructure/celery_app.py`. This directory is reserved for shared worker deployment material; no domain task is defined yet.
